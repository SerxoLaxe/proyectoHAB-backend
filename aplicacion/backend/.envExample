#Host y puerto de escucha para Express.
HOST=
PORT=

#Directorio en el que se guardarán los archivos estáticos (imágenes principalmente).
UPLOAD_DIRECTORY=

#Variables relativas a la base de datos.
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_DATABASE=

#Si el valor de RESET_DB es true las tablas definidas en el código se eliminaran si existen y se crearán de nuevo para ser rellenadas con datos generados por el módulo Faker. Si el valor en cambio es false las tablas sólo se crearán si no existen y nunca serán rellenadas con datos generados programáticamente.
RESET_DB=

#Contraseña admin
ADMIN_NAME=
ADMIN_EMAIL=
ADMIN_PASSWORD=

#Configura el log de errores para mostrar el stack del error por completo.
VERBOSE=false